package purebe.battlemage.magic;

import net.minecraft.entity.player.EntityPlayer;
import net.minecraft.util.BlockPos;
import net.minecraft.util.Vec3;

public class Teleport implements ISpell {
	final EntityPlayer     player;
	final Vec3             playerLookat;
	final int              intensity;
	final int              max_distance;
	final int              min_distance;
	
	public Teleport(EntityPlayer player, Vec3 playerLookat) {
		this.player = player;
		this.playerLookat = playerLookat;
		this.intensity = 1;
		this.max_distance = 40;
		this.min_distance = 10;
	}
	
	@Override
	public void cast() {	
		Vec3 telePosition = new Vec3(player.posX + playerLookat.xCoord * max_distance,
		                       player.posY,
		                       player.posZ + playerLookat.zCoord * max_distance);
		
	}
	
	protected boolean isHeadroom(Vec3 position) {
		if (player.worldObj.getBlockState(new BlockPos(position.xCoord, position.yCoord, position.zCoord))) {
			
		}
	}
}
