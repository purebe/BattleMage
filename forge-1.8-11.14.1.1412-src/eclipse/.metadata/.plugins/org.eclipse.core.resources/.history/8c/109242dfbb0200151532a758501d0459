package purebe.battlemage.entities;

import net.minecraft.entity.passive.EntityCow;
import net.minecraft.world.World;

public class ArtilleryCattleEntity extends EntityCow {
	protected final float airDragFactor = 0.1f; 
	public ArtilleryCattleEntity(World worldIn) {
		super(worldIn);
	}
	
	@Override
	public void onLivingUpdate() {
		super.onLivingUpdate();
		
		if (this.motionX > 0.0 && this.motionZ > 0.0) {
			this.addVelocity(airDragFactor, 0, airDragFactor);
		}
		else if (this.motionX > 0.0) {
			this.addVelocity(airDragFactor, 0, 0);
		}
		else if (this.motionZ > 0.0) {
			this.addVelocity(0, 0, airDragFactor);
		}
		
		if (this.ticksExisted > 60) {
			this.worldObj.createExplosion(null, posX, posY, posZ, 8, false);
		}
	}
}
